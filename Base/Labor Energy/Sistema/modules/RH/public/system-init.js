/* ========================================= *//* INICIALIZAÇÁO DO SISTEMA ADMINISTRATIVO *//* ========================================= */document.addEventListener('DOMContentLoaded', function() {    // Aguardar carregamento completo    setTimeout(function() {        // Verificar se headerControls existe        if (typeof window.headerControls !== 'undefined') {            // Carregar dashboard por padráo            loadPage('dashboard');                        // Sistema carregado (sem notificações automáticas)        } else {            console.warn('⚠️ Header Controls náo encontrado, tentando novamente...');                        // Tentar novamente após 1 segundo            setTimeout(() => {                if (typeof window.headerControls !== 'undefined') {                    loadPage('dashboard');                } else {                    console.error('❌ Erro: Header Controls náo pôde ser carregado');                }            }, 1000);        }                // Verificar se a funçáo loadPage existe        if (typeof loadPage === 'function') {        } else {            console.error('❌ Erro: Sistema de páginas náo encontrado');        }            }, 500);});// Adicionar listener para clicks nos links do menudocument.addEventListener('click', function(e) {    const link = e.target.closest('.nav-link');    if (link && !link.classList.contains('logout-link')) {        e.preventDefault();                // Remover active de todos        document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));                // Adicionar active no clicado        link.classList.add('active');    }});// Funçáo para demonstrar mensagens (carta)function showMessagesDemo() {    if (window.headerControls) {        window.headerControls.addNotification('Nova mensagem recebida!', 'info');        window.headerControls.addNotification('Você tem 3 mensagens náo lidas', 'warning');    } else {        console.warn('Sistema de mensagens náo disponível');    }}// Funçáo para teste rápido do sistemawindow.testSystem = function() {    if (window.headerControls) {    } else {        console.error('❌ Teste falhou: headerControls náo encontrado');    }};