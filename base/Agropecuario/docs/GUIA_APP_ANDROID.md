# 📱 ALUFORCE - Guia Completo do Aplicativo Android## 🎯 Visão GeralEste guia explica como gerar e instalar o aplicativo Android do sistema ALUFORCE. O app funciona exatamente como a versão web, com todas as funcionalidades:- ✅ Dashboard com indicadores em tempo real- ✅ Módulo de Vendas (Kanban, pedidos, clientes)- ✅ Módulo de Compras (fornecedores, cotações)- ✅ Módulo Financeiro (contas a pagar/receber, fluxo de caixa)- ✅ Módulo PCP (ordens de produção, materiais)- ✅ Módulo RH (funcionários, folha de ponto)- ✅ Módulo NFe (emissão de notas fiscais)- ✅ Chat interno entre colaboradores- ✅ Notificações push- ✅ Câmera para upload de documentos---## 🛠️ Requisitos### Para Gerar o APK:- **Node.js** 18+ (https://nodejs.org)- **Java JDK** 17+ (https://adoptium.net)- **Android Studio** (opcional, mas recomendado)### Para Instalar o APK:- Celular Android 7.0+ (API 24)- Ativar "Fontes desconhecidas" nas configurações---## 📦 Como Gerar o APK### Método 1: Script Automático (Recomendado)1. **Execute o script de build:**   ```   BUILD_APK_ANDROID.bat   ```2. **Escolha a opção:**   - `[1] Build DEBUG` - Para testes   - `[2] Build RELEASE` - Para distribuição3. **Aguarde o processo**4. **O APK estará em:**   ```   APK-OUTPUT\ALUFORCE-DEBUG.apk   ```### Método 2: Via Android Studio1. **Abra o Android Studio:**   ```   abrir_android_studio.bat   ```2. **Aguarde o Gradle sincronizar**3. **Gere o APK:**   - Menu: `Build > Build Bundle(s) / APK(s) > Build APK(s)`4. **Localize o APK:**   ```   android\app\build\outputs\apk\debug\app-debug.apk   ```### Método 3: Via Terminal```powershell# Sincronizar arquivos webnpx cap sync android# Entrar na pasta Androidcd android# Gerar APK Debug.\gradlew.bat assembleDebug# Gerar APK Release.\gradlew.bat assembleRelease```---## 📲 Como Instalar no Celular### Opção 1: Via USB (ADB)1. **Ative a depuração USB no celular:**   - Configurações > Sobre o telefone > Toque 7x em "Número da versão"   - Configurações > Opções do desenvolvedor > Depuração USB2. **Conecte o celular no PC**3. **Execute:**   ```   adb install APK-OUTPUT\ALUFORCE-DEBUG.apk   ```### Opção 2: Transferência Manual1. **Copie o APK para o celular** (via cabo, email, Drive, etc.)2. **No celular:**   - Abra o arquivo APK   - Permita a instalação de fontes desconhecidas   - Toque em "Instalar"### Opção 3: QR Code (Rede Local)1. **Inicie o servidor:**   ```   node server.js   ```2. **Acesse no celular:**   ```   http://192.168.68.133:3000/apk   ```   (Ajuste o IP para o do seu computador)---## ⚙️ Configuração do ServidorO app pode se conectar a diferentes servidores. Edite o arquivo `capacitor.config.ts`:### Servidor Local (Rede WiFi):```typescriptconst SERVER_MODE: 'local' | 'production' | 'embedded' = 'local';```### Servidor de Produção (Internet):```typescriptconst SERVER_MODE: 'local' | 'production' | 'embedded' = 'production';```### Modo Offline (Arquivos Locais):```typescriptconst SERVER_MODE: 'local' | 'production' | 'embedded' = 'embedded';```**Após alterar, sincronize:**```npx cap sync android```---## 🔧 Personalização### Alterar Ícone do App1. **Substitua os ícones em:**   ```   public/icons/icon-512x512.png   ```2. **Execute:**   ```   node preparar_android.js   ```3. **Sincronize:**   ```   npx cap sync android   ```### Alterar Nome do AppEdite `android/app/src/main/res/values/strings.xml`:```xml<string name="app_name">ALUFORCE</string>```### Alterar CoresEdite `android/app/src/main/res/values/colors.xml`:```xml<color name="colorPrimary">#1a2744</color>```---## 📋 Estrutura do Projeto Android```android/├── app/│   ├── src/main/│   │   ├── AndroidManifest.xml    # Configurações do app│   │   ├── assets/                # Arquivos web (após sync)│   │   ├── java/                  # Código Java│   │   └── res/                   # Recursos (ícones, strings)│   └── build.gradle               # Dependências├── build.gradle                   # Configuração raiz└── variables.gradle               # Versões do SDK```---## 🚀 Deploy para Play Store### 1. Criar Keystore de Release```bashkeytool -genkeypair -v -keystore aluforce-release.keystore -alias aluforce -keyalg RSA -keysize 2048 -validity 10000```### 2. Configurar Signing no GradleEdite `android/app/build.gradle`:```groovyandroid {    signingConfigs {        release {            storeFile file('aluforce-release.keystore')            storePassword 'sua_senha'            keyAlias 'aluforce'            keyPassword 'sua_senha'        }    }    buildTypes {        release {            signingConfig signingConfigs.release            minifyEnabled true            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'        }    }}```### 3. Gerar Bundle para Play Store```bashcd android.\gradlew.bat bundleRelease```O bundle estará em: `android/app/build/outputs/bundle/release/app-release.aab`---## 🐛 Solução de Problemas### Erro: "SDK location not found"```bash# Crie o arquivo android/local.properties com:sdk.dir=C:\\Users\\SEU_USUARIO\\AppData\\Local\\Android\\Sdk```### Erro: "Gradle build failed"```bashcd android.\gradlew.bat clean.\gradlew.bat assembleDebug```### App não conecta ao servidor1. Verifique se o servidor está rodando (`node server.js`)2. Verifique se o celular está na mesma rede WiFi3. Verifique o IP em `capacitor.config.ts`### Tela branca no app1. Abra o Chrome no PC2. Acesse: `chrome://inspect/#devices`3. Conecte o celular via USB4. Clique "inspect" no WebView do app5. Verifique erros no Console---## 📱 Funcionalidades Mobile### Permissões Solicitadas:- **Internet** - Comunicação com servidor- **Câmera** - Upload de fotos/documentos- **Arquivos** - Download de relatórios- **Notificações** - Alertas do sistema### Gestos:- **Deslizar da esquerda** - Abrir menu lateral- **Botão Voltar** - Fecha modais ou volta página- **Pull to Refresh** - Atualiza dados (em algumas telas)---## 📞 SuporteEm caso de problemas:1. Verifique os logs: `adb logcat | grep ALUFORCE`2. Console do Chrome: `chrome://inspect`3. Contate o suporte técnico---**Versão:** 2.1.7  **Última Atualização:** Janeiro 2026  **Compatibilidade:** Android 7.0+ (API 24)